    import XCTest
    @testable import MMActionSheet

    final class MMActionSheetTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            // XCTAssertEqual(MMActionSheet().text, "Hello, World!")
        }
    }
